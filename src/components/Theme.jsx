import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#2596be",
    },
    secondary: {
      light: "#F5F5F5",
      main: "#FFF",
      contrastText: "#F5F5F5",
    },
    neutral: {
        main: '#2596be',
        contrastText: '#fff',
      }
  }
});
